<div class="container mt-6">
  <div class="rectangle">
  <h1>Your Recipes</h1>
  <p>Theses are your favorites recipes
  </p>
  <ul>
  <% @recipes.each do |recipe| %>
    <li class="favList">
      <%= link_to recipe.title, recipe_path(recipe),class:'favRecipes' %>
    </li>
  <% end %>
</ul>
  <!-- app/views/recipes/myRecipes.html.erb -->
  </div>
  <!-- Add more content here -->
</div>
